# @out(path=config/env.go)
# @package=config

# @sensitive @required @default=1
KEY=VALUE

# @type=int @docs=test
TEST=VALIUE-2

# @type=string(startsWith=sk_)
IDK=sk_WHOQMI

# @type=enum(dev,stage)
ENV=stage